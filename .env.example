# Vercel KV — set these via Vercel dashboard or `vercel env pull`
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Absolute site URL — used so share-card images unfurl on WhatsApp/social.
# e.g. https://your-domain.com  (no trailing slash)
NEXT_PUBLIC_SITE_URL=
